# 북유럽 선수명 오음역 정리 (2026-08-07)

영문 `name` 복구([checklist.md](checklist.md))로 비로소 가능해진 후속 작업.
`scripts/audit-nordic-player-names.ts`.

## 대상 판정 — 국적으로 먼저 가른다

같은 철자라도 언어권이 다르면 규칙이 정반대다. 이걸 안 가르면 대량 개악이 난다.

| 언어권 | `hj` 규칙 | 처리 |
|---|---|---|
| 노르웨이·덴마크·스웨덴·페로 | **h 묵음** (Hjalmar → 얄마르) | 규칙 교정 대상 |
| **아이슬란드** | **h 살아 있음** (/ç/) | 규칙 교정 **제외** |
| 알바니아·마케도니아·세르비아 | 별개 규칙 | 제외 |

아이슬란드어가 예외라는 건 ko위키 정본 `Hjörtur Hermannsson → 히외르튀르` 가 확증했다.
ISL 선수 5명 전부 h 를 살린 표기가 정상이라, 처음의 일괄 적용안대로 갔으면 이들이 전부 망가졌다.

## 적용 방침

1. **ko위키 langlink 정본은 국적 무관 무조건 채택.** 언어 규칙 판단이 필요 없다.
2. 위키가 없으면 **현재 값이 hj 규칙을 명백히 위반할 때만** haiku 음역을 쓴다.
3. 위반이 아닌데 haiku 가 손보자는 `Kj`·`Sj` 미세조정은 **전부 버린다**(보류 35건).
4. haiku 값을 쓸 때도 **위반 토큰만 갈아끼우고** 나머지 토큰은 현행 유지.

3·4는 실측 근거가 있다. haiku 는 `얄테 기츠`(정상)를 `알테 기츠`(j 탈락)로 되돌리려 했고,
같은 입력에도 `Andrew` 를 실행마다 앤드루/안드레로 다르게 냈다.

## 함정 두 개 (둘 다 조용히 실패한다)

- **Wikipedia API 는 User-Agent 없는 요청을 막는다.** 헤더가 없으면 예외가 아니라 빈 응답이 와서
  58건 전건이 "위키 문서 없음"으로 조용히 처리됐다. `scorebase-bot/1.0` UA 필수.
- **한글 범위는 `[하-힣]` 이어야 한다.** `[하-히]` 는 받침 있는 ㅎ 음절(`힐`·`혤`·`휼`)을 놓쳐
  "힐테 비드스트룹"·"욘 올라브 혤데"가 검사를 통과했다.

## 결과

자동 11건 + 수동 5건 = **16건 교정**, DB `nameKo`·`data/player-ko-locks.json` 양쪽 반영.

- 위키 정본 4건 — 모르텐 율만(스포르팅 CP)·시몬 키에르(AC밀란)·히외르튀르 헤르만손·조코 자이코프·클라우스 자술라
- 규칙 교정 — `Hjalte` 6명이 얄테/힐테/발테로 갈렸던 것을 **전부 "얄테"로 통일**
- 수동 — `N. Hjörvarsson → "음욕"`(봇 산출물)은 영문이 이니셜뿐이라 음역 근거가 없어 `nameKo` 를
  비웠다(화면은 영문 폴백). locks 엔 `ko: ""` 로 등재해 봇 재삽입만 막았다.
- 수동 — `Jon Olav Hjelde`. **ko위키 표제어가 "욘 올라브 혤데" 라 자동 로직이 손대지 않았다.**
  아들 `Leo Fuhr Hjelde` 를 "옐데"로 확정한 터라 부자 성 표기가 갈려, 위키를 거스르고 "옐데"로 통일했다.
  근거는 노르웨이어 hj 묵음(같은 위키의 Hjalmar → 얄마르 langlink)과 부자 일관성. **되돌리려면 locks 에서
  이 id 를 고치면 된다.**
- 전파 정리 — `player-overrides.json`(OV 가 DB보다 우선이라 이걸 안 고치면 화면이 안 바뀐다) 1건,
  발행 글 `Blog` 본문 1건(`transfer-weekly-2026-07-13`).

## 손대지 않은 것

- **`Kj`·`Sj`·`Skj` 계열 35명** — 현재 표기가 규칙 위반은 아니고, 노르웨이어(셰)와 덴마크어(키)가
  갈려 일괄 적용이 위험하다. 위키 문서도 없다. 사람이 언어권별로 훑어야 하는 영역.
- **국적 미상 15명** — TheSports 가 nationality 를 안 준 선수. 언어 판정이 안 서 규칙 교정에서 뺐다.
