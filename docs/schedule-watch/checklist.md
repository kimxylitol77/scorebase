# 일정 감시·자동 복구 루프 (schedule-watch) — 체크리스트

목표: 9/24~25 수동 감사에서 찾은 유형을 기계가 매일 찾고, 고칠 수 있는 건 고치고, 못 고치는 것만 한 번에 알린다.

## 1단계 — 축구 (af 일정 피드 기준, Vercel cron 6h)
- [x] 매칭 순수 함수 분리 + 테스트 (af 경기 ↔ DB 행: ext id / af 팀 id 양쪽 / ±3h 한 팀 확정 / 단어 겹침)
- [x] 탐지 A — af 수집 리그에서 빠진 경기 → 자동 치유: 해당 리그·날짜 collect 재실행 → 재검증
- [x] 탐지 B — TheSports 수집 리그에서 빠진 경기 → 알림(리그별 묶음 + 실행할 명령)
- [x] 탐지 C — 노출 리그인데 창 안 DB 0건·af 3건+ (수집 경로 없음·ts 조 이름 변경 등) → 알림
- [x] 탐지 D — TS_COVERED af 행 미래 날짜 고착(21일 내) → 자동 치유: af 날짜 추종(쌍둥이·겹침 가드)
- [x] 탐지 E — 한 팀 3시간 안에 다른 상대와 두 경기(오매핑 신호) → 알림
- [x] 루프 안전장치 — 치유 key 당 7일 3회 상한 → "사람 개입 필요" 에스컬레이션, 같은 알림 24h 1회
- [x] vercel.json cron + CRON_REGISTRY 등록
- [ ] tsc · npm test · 운영 1회 호출 검증 · push

## 1단계 dry-run 실측 (2026-09-25)
- af 538경기 → 누락 5(전부 실누락: WALES_PL·WK_LEAGUE·MOROCCO_BP) · 날짜 교정 174(표본 전부 시즌 초 임시시각→실제 시각) · 교정 보류 3(사우디, af·DB 날짜 충돌) · 오매핑 의심 2(Cardiff·Aris 중복 행)
- 규칙 보강 2건: 킥오프 ±15분 + 한 팀 엄격 이름 일치(MLS LAFC 헛누락), 괄호 국가 꼬리표 무시(파나마 "(PAN)")

## 2단계 — 전 종목 "팀 매핑 없어 버려진 경기"
- [x] 설계 변경: TheSports 재조회 대신 버리는 자리에서 기록 — 하키·농구·야구·배구 워커는 매핑 없는 경기를 사이트로 보내기 전에 거르고, 축구는 수신 라우트가 skippedNoTeam 으로 버린다. 둘 다 무음이었다(하키 친선 144경기)
- [x] 워커 4개: 거른 경기를 unmapped 로 보고(수집 동작 불변) · 라우트: 대체 행에 캐시만 이은 경우 제외하고 기록
- [x] 기록 = HealthCheck(category ts-unmapped, key 리그) 최신 스냅샷, 경기 id 기준 병합·지난 경기 3일 보존
- [x] schedule-watch: 26h 안에 갱신된 스냅샷 중 아직도 DB(ts 행·캐시 연결)에 없는 경기만 알림 — 가짜 리그로 실DB 검증 후 삭제
- [ ] 워커 4개 Vultr 배포·재시작, 운영 호출 검증
