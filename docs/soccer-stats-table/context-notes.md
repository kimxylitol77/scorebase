# 컨텍스트 노트
- 09-23 사용자 "진행해줘" — 야구 표 확장(MLB) 다음 순서로 착수.
- xG 는 EPL·세리에A 히트맵 소스에만 있어 열에서 제외. 온오프·임팩트 열은 재료 없음(NBA 와 같은 이유).
- 아카이브는 ts 가 정본(af 아카이브는 2025-26 라 낡음). 리그별 시즌 라벨이 "2026-27"(유럽) 과 "2026"(달력) 으로 갈리니 리그마다 최댓값을 고른다.
- 평점은 PlayerMatchLog 가 af 리그만 커버(K리그1 평점 3.92 는 결측 섞인 값 → 리그별 커버리지 낮으면 열 숨김 검토).
- 09-23 구현. 실측(로컬): EPL 규정 182분 217명(전체 382), GK 21명, K리그1 1044분 136명, MLS FW 90분당 84명. 팀명은 toKoreanTeamName 으로 한글화(아카이브 stat.team 은 영문).
- 평점 신뢰 게이트: 리그 커버리지 30% 미만 또는 평균 5 미만이면 평점 열을 통째로 비움(K리그1 은 로그 결측이 0 으로 섞여 평균 3.9 → 비움). EPL 은 홀란 7.75·이사크 7.26 정상.
- 야구 표의 percentile·sortStatRows·formatStat·StatColumn 을 그대로 import. 화면은 야구 페이지 복제(표 컴포넌트 공용화는 후속).

