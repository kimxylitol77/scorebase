# 고확신 픽 — 체크리스트

## 1단계 · 예정 경기 예측 생성 (선결 과제) — 완료 2026-08-03

- [x] 기존 계산 함수 시그니처 확인 — `buildMatchContext` · `predictTotalMarket` · `predictHandicapMarket` · `bestDoubleChance`
- [x] `src/lib/predict/compute-prediction.ts` — 계산부 공용 모듈 (evaluate 와 같은 순서)
- [x] `src/jobs/predict-upcoming.ts` — 예정 경기 1X2 + 핸디 + OU + DC 계산·저장
- [x] look-ahead 방지 — 각 함수에 `asOf = startTime` 전달
      → **검증 결과: 핸디·OU·DC 는 종료 경기 60건 전수 소수점까지 일치.** 1X2 만 0.5~2%p
        차이가 나는데, 계산 경로가 아니라 입력(선발·배당)이 경기 후 갱신된 탓이다.
- [x] 이미 예측이 있는 경기는 건드리지 않음 (`--force` 로만 덮어씀) — 본 픽이 바뀌면 신뢰를 잃는다
- [x] 실제 저장 — 72h 창 예측 보유 **4건 → 162건**, 고확신 **0건 → 25건**
- [x] cron 배선 — `/api/cron/predict-upcoming`, `0 3,13 * * *` UTC (= 12:00·22:00 KST, 선발 갱신 직후)

### 1단계에서 함께 고친 것 — 채점이 사전 픽을 덮어쓰던 문제

`evaluate-predictions` 의 `useStored` 가 **PREVIEW 글이 있는 경기만** 저장된 픽으로 채점하고,
나머지는 채점 시점에 재계산한 값으로 `predHome`/`predWinner` 를 덮어썼다.
실측 — 채점 11,611건 중 **9,449건(81%)이 사후 재계산**이었고 핸디·OU·DC 는 100% 사후였다.
사전 픽을 보여주기 시작하면 "보여준 픽 ≠ 채점된 픽" 이 되므로 `useStored = predWinner != null` 로 확장.
적중률 영향은 작다(사전 53.0% vs 사후 53.8%). 이미 채점된 과거 데이터는 소급 변경 없음.

## 2단계 · 고확신 픽 선정 로직 — 완료 2026-08-03

- [x] 마켓별 임계 확정 — 핸디 65%+ · DC 65%+ · 1X2 80%+ · OU 80%+ · 양팀득점 제외
- [x] `src/lib/predict/strong-picks.ts` — 임계 + 선정 함수 (표시·집계 공용 단일 출처)
- [x] 과거 데이터 재적용 검증 — **종합 71.0%** (7,380픽 중 5,240적중)
      마켓별: 더블찬스 72.3%(2003) · 핸디캡 70.5%(3960) · 승부 71.3%(703) · 오버언더 70.2%(714)
      픽이 나온 경기 6,269 / 11,611 (54%) — 절반은 아예 픽을 내지 않는다

## 3단계 · /picks/strong 페이지 — 완료 2026-08-03

- [x] 페이지 + 데이터 로더(`_data.ts`) — 72h 창, 1차 DB 필터 후 selectStrongPicks 로 확정
- [x] 회원 게이트 — 비회원은 개수만(`N경기 M건`) + 가입 CTA, 픽 내용 비공개
      → 검증: 비회원/회원 양쪽 실렌더 확인. 회원 화면에서 CTA 사라지고 픽 노출
- [x] 빈 상태 — 임계를 99.9%로 올려 재현, "없는 날은 없는 대로 둡니다" 안내 확인 후 원복
- [x] 적중률 근거 표시 — 상수가 아니라 DB count 실측(마켓별 2회씩, 전수 스캔 회피)
- [x] 네비 추가 — 커뮤니티 메뉴에 "고확신 픽"
- [x] tsc + lint + 콘솔 에러 0

## 4단계 · 마무리

- [x] 프로덕션 배포 후 실렌더 — 비회원 확인 완료(적중률 71.0%/7,380픽·24경기 25건·CTA·픽 유출 0)
- [ ] 프로덕션 **회원 화면** 미확인 — prod USER_SESSION_SECRET 이 로컬과 달라 쿠키 생성 불가. 사용자가 직접 로그인해 확인 필요
- [x] 메모리 기록 — strong-picks-feature

## 5단계 · 픽 발행 시점 원칙 — 완료 2026-08-03

**원칙 (사용자 확정): 야구는 선발 투수, 하키는 골리가 확정됐을 때만 픽을 발행한다.** 적중률을 높이기 위한 것.
축구는 라인업이 대체로 고정이므로 부상자 명단이 확정되는 킥오프 하루 전에 낸다.

- [x] `src/lib/predict/pick-readiness.ts` — 판정 단일 기준. 픽을 저장·발행하는 모든 경로가 통과
- [x] `predict-upcoming` 적용 — 대기 사유(선발·골리 미확정·하루 이상 남음)를 집계로 남김
- [x] `generate-previews` 적용 (`baseballOnly`) — MLB·KBO·NPB 는 이미 2026-06-01 게이트가 있어
      도달하지 않고, 이 가드는 CPBL·LMB 등 나머지 야구 리그를 덮는다. Article.pred* 도 같이 비움
- [x] 발행 경로 전수 확인 — broadcast-channel·post-daily-topic 등은 저장값을 읽어 내보내므로
      저장 지점(위 2곳)을 막으면 전파된다
- [x] 원칙 위반으로 이미 박힌 픽 110건 삭제 (선발 미확정 82 · 하루 이상 남음 28)
- [x] 단위 검증 — 야구 5리그 × 4케이스 · 축구 3리그 · 그 밖 종목, 전부 통과
- [x] 재실행 실측 — 계산 49건, 대기 선발 미확정 83 · 하루 이상 70

### 영향
지금 고확신 픽은 3경기 3건뿐이다. 야구가 전부 선발 대기로 빠졌기 때문이며, 선발이 발표되면
(KBO/NPB 오전·MLB 저녁 cron 직후) 다시 들어온다. **이게 원칙대로 동작하는 모습이다.**

## 보류 · 확인 필요

- ~~NHL 골리~~ → **적용 완료**(2026-08-03). 야구 선발과 동일 규칙. 오프시즌이라 지금은 영향 0, 10월부터 실제 작동
- 부상자를 승률 계산에 넣는 건 **백테스트 불가** — 축구 부상자가 DB에 없고 in-memory 캐시뿐이라
  과거 시점 데이터가 없다. 사용자 결정 = 스냅샷부터 쌓고 몇 달 뒤 백테스트 (**미착수**)
- 비시즌(~10월) 동안 픽이 적은 것은 계절성 — 임계를 낮추지 않기로

## 6단계 · 화면 개편 (로고·사유·시각화·날짜별 결과) — 2026-08-04

사용자 요청 네 가지. 결정 두 개는 물어서 확정했다.
- 지난 픽(결과 확정)도 **회원 전용** 유지 — 현행 방침 그대로
- 사유 재료 = **저장값 + 최근 폼** (Elo 는 야구·농구가 전부 1500 기본값이라 근거로 못 씀)

- [x] 팀 로고 — `TeamBadge` + `homeTeam.logoUrl` select 추가 (예정 6경기 전부 로고 보유 실측)
- [x] 사유 — 임계 초과폭 / 그 기준의 과거 실측 / 시장 대비 괴리 / 선발 ERA / 최근 5경기 폼
- [x] 시각화 — 확신도 막대(임계선 마커) + 1X2 분할 바 + 날짜별 성적 막대
- [x] 날짜별 보기 — `?d=YYYY-MM-DD`, 지난 픽은 적중/실패 O·X 표기
- [x] look-ahead 금지 — 과거 날짜의 폼은 **그 경기 시작 시각 이전** 경기만
- [x] tsc 통과 + 로컬 실렌더(비회원·회원 양쪽)
- [ ] 커밋·push
