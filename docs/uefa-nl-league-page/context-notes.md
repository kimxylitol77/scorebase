# 컨텍스트 노트 — UEFA 네이션스리그 페이지

## 2026-09-24 착수

**왜 지금인가.** 대회 개막일이 오늘이다(첫 경기 KST 9/25 01:45). 구조를 지금 만들어야
첫 라운드부터 데이터가 쌓인다.

**UEFA_NL 은 어제 처음 수집됐다.** `national-team-collect-gap` 메모리 참고 — collect 라우트
주석엔 "af 계속 사용"이라 적혀 있었지만 `ALL_LEAGUES` 에 없어 한 번도 수집된 적이 없었다.
9/23 에 등록돼 9/24~10/1 7일치 60경기가 처음 들어왔다.

**조별 표 렌더는 이미 있다.** `/standings/[league]/page.tsx:452` 가 ts 표를 group 번호로
갈라 A조·B조 섹션으로 그린다. 문제는 이 경로가 ts 전용이라는 것. af 는 소스 분기에 없다.
그래서 UEFA_NL 은 `source = "calc"` 로 떨어지고, DB 종료 0건이라 Team row 3개만 남는다.

**af 그룹 라벨이 길다.** `"UEFA Nations League , League A, Group 1"` 처럼 대회명이 접두로
붙고 쉼표 앞에 공백이 있다. 파싱할 때 대회명을 떼고 League/Group 만 뽑아야 한다.

**전수 조사에서 같은 증상은 UEFA_NL 뿐이었다.** AFCON 이 48팀 평면 표로 보여서 처음엔
같이 깨진 줄 알았는데, 실제로는 ts 조별 표 12개가 이미 있고 전부 0경기(2027 대회)라
"지난 시즌 최종 순위" 라벨을 달고 대기 중인 정상 동작이었다. 판단 전에 ts 표의 경기수
합계를 반드시 확인할 것 — 화면만 보면 오진한다.

**Team row 가 적은 건 UEFA_NL 만의 문제가 아니다.** AFC_CL 3개, COPA_SUD 2개, GULF_CUP 1개
등 컵 대회 전반이 낮다(`uefa-qualifier-team-mapping-gap`). 다만 이들은 ts 표가 있어 화면이
멀쩡하다. Team row 는 순위가 calc 로 떨어질 때만 표면화된다.

## 2026-09-24 UI 재설계 — databallr 문법을 우리 디자인 시스템으로

**참고 대상.** databallr.com/playoffs — 단일 초점 매치업 카드, 양옆 결과 레일, 큰 팀명, 7칸 시리즈 진행 표시.
그대로 옮길 수 없는 이유는 저쪽이 다크 전용이라서다. 우리는 라이트(로즈 흰 카드)·다크(elevated) 둘 다 지원한다.
글로우는 빅매치 뒤 로즈 스포트라이트 한 곳에만 쓰고(라이트 rose-300/30, 다크 rose-500/14), 나머지는 조용히 뒀다.

**빅매치 선정.** 진행 중 리그 A 경기 > 아직 안 치른 가장 이른 라운드에서 두 팀 FIFA 순위 합이 가장 작은 경기 >
리그페이즈 종료 후엔 마지막 경기. 시계(Date.now)가 아니라 수집기가 관리하는 status 를 정본으로 본다.
개막 라운드 결과: 잉글랜드(4위) vs 스페인(1위).

**레일 = 같은 라운드 리그 A 나머지 경기**, 1·2조 왼쪽·3·4조 오른쪽(databallr 의 West/East 대응, 조 번호라는 사실을 담는다).
경기 수가 4 대 3이라 가운데 정렬하면 머리글이 어긋나서 items-start 로 세 머리글을 한 줄에 맞췄다.

**큰 자리엔 국기 이모지 대신 협회 엠블럼 이미지.** 윈도우는 국기 이모지를 "NL" 같은 글자로 그린다. 작은 행은
사이트 표준(이모지·aria-hidden)을 따르고, 72px 짜리 초점 카드만 Team.logoUrl(af 엠블럼)을 쓴다.
북아일랜드는 공식 국기 이모지가 없어 회색 자리표시로 정렬만 맞춘다.

**구역 색은 확정 규칙만.** 리그 A 조 1·2위 8강, 리그 B·C·D 조 1위 승격(UEFA 공식 2026-27 규정).
하위권 강등·승강 PO 는 조 간 비교라 행 단위로 칠하면 틀린다 — 문구로만 설명한다.
그리고 **조에서 한 경기도 안 치렀으면 색을 칠하지 않는다.** 개막 전 af 순서는 임의라 프랑스·이탈리아에
8강 막대가 붙으면 확정처럼 읽힌다.

**리그 A~D 토글은 클라이언트 상태.** 네 등급 데이터가 모두 서버 HTML 에 실려 검색엔진은 54팀 전부를 본다.

**검증은 Playwright MCP** — 데스크톱·모바일(390)·다크·토글 클릭(aria-pressed) 확인, 콘솔 오류 0.
dev 첫 요청 500 은 preview 가 홈(/)을 먼저 열어 커넥션 풀을 소진한 것(오류 30건 전부 홈 컴포넌트).
로컬 빌드는 Neon 응답이 5초대로 느린 날 prerender 에서 P1001 로 두 번 실패, 3차에 통과 — 코드 문제 아님.

## 2026-09-24 빅매치 허브 확장 — AFCON·걸프컵

**부품 공용화.** `components/leagues/hub/`(SpotlightSection·GroupSegments·HubParts·types) + 규칙
`lib/sports/tournament-hub.ts`. 대회별 허브는 데이터만 채운다(NationsLeagueHub·AfconHub·GulfCupHub).

**빅매치 기준을 바꿨다.** 순위 합 최소 → **약한 쪽 순위가 가장 높은 경기(둘 다 강함)**, 동률은 순위 합.
AFCON 1라운드에서 순위 합 기준은 모로코(6)–가봉(84) 일방적 경기를 뽑았다. 새 기준은 말리(53)–카보베르데(64).
네이션스리그는 잉글랜드–스페인 그대로.

**AFCON 2027 예선.** 12개 조 × 4, 6라운드(9~10월/11월/2027년 3월). 조 1·2위 본선, 단 공동개최국 케냐(D)·
우간다(H)·탄자니아(L)는 자동 진출이고 그 조는 개최국 뺀 최상위 1팀(CAF 공식). ts 조 번호 1~12 = A~L 은
개최국 위치로 검증했다. 종료 12경기는 3월 예비예선이라 조별 경기(두 팀 같은 조)만 쓴다.
13개국 영문명 → 한글 사전 추가(레소토·가봉·남수단·… 사이트 전역 반영).

**AFCON 페이지의 "디펜딩 챔피언 부룬디" 오보.** 개막 전 팩트카드가 "지난 시즌 표 1위 = 우승"으로 계산했다.
토너먼트는 성립하지 않는다 → STAGED_COMPETITIONS 는 league-champions.json 기록이 있을 때만 챔피언 카드,
"승격·새 얼굴"은 아예 안 낸다(LeagueStandingsTable).

**걸프컵.** 리그 페이지가 네이션스리그 처음처럼 "순위 데이터를 수집 중"(VALID_LEAGUES 누락). af·ts 모두 순위표 없음
(af coverage.standings=false) → 종료 경기로 직접 계산. 조 이름도 데이터에 없어 개최국 사우디 포함 조를 A조로
(공식 추첨 A 사우디·이라크·오만·쿠웨이트 / B 바레인·UAE·카타르·예멘). 자체 계산이라 동률 규정(상대 전적 등)이
다를 수 있어 **세 번째 줄과 완전 동률인 팀은 4강권 표시 보류**. 순위 번호로 비교하면 2위 동률 두 팀이 둘 다
칠해지는 구멍이 있어 줄 순서로 본다. `/standings/GULF_CUP` 은 표를 못 그려 "순위표 전체" 칩을 뺐다.

**경기 전 조는 순위 칸 "–".** af·ts 기본 순서도, 자체 계산 전원 동률도 순위가 아니다.

**아직 안 한 토너먼트.** 녹아웃 컵(FA컵·EFL컵·코파 이탈리아 등)은 조가 없어 카드 그리드 대신 대진표가 들어가야 하고,
빅매치 기준도 FIFA 순위가 아니라 클럽 Elo 여야 한다. UCL·UEL 은 36팀 단일 표 + 이미 전체 탭. 휴면 대회
(월드컵·골드컵 등)는 지금 데이터로 검증할 수 없다.

## 2026-09-24 녹아웃 컵 — "대회 여정" 카드 (사용자 선택: 허브 대신 여정)

**왜 빅매치 허브가 아닌가.** 녹아웃 컵은 지금 다가오는 경기가 거의 0건(르방컵만 16건)이라 빅매치 자리에 이미 끝난
비리그 경기가 올라간다. 경기가 없어도 의미 있는 화면 = 진행 라운드 트랙 + 빅클럽 합류 + 우승 기록 + 라운드별 결과.

**FA컵에 라운드 이름이 없던 이유.** ts 수집기(football-collector)는 경기 객체를 raw 에 그대로 넣고 round 엔 stage_id 만 있다.
라운드 이름을 붙이는 건 워커 경로(thesports-matches, {"thesports":{"round":{stageName}}})뿐이었다.
collect.ts 는 수집마다 raw 를 새로 쓰므로(451행) 백필만 하면 다음 수집에 지워진다 → 수집기가 토너먼트
(STAGED_COMPETITIONS) 경기에 stage/list 로 이름을 붙이게 고치고, 기존 FA컵 446경기 백필(3경기는 ts 가 stage 정보 없음).
raw 형태가 셋이다: af {"league":{"round"}} / ts 워커 {"thesports":{"round":{stageName}}} / ts 수집기 {"round":{stage_id,stageName}}.

**데이터에 맞추고 공식 체계에 끼워 맞추지 않는다.** ts 는 FA컵 예선을 "First/Second/Third Preliminary Round"라 부르는데
FA 공식은 추가예선·예비예선 다음 1~4차 예선이다. 추가예선도 우리 DB 엔 8경기뿐(실제 약 200). 트랙은 데이터에 있는 라운드를
첫 경기 날짜 순으로, 이름은 소스 이름을 그대로 옮긴다. "수집한 경기 기준"·"하부 예선 일부만 수집"을 화면에 밝힌다.

**시즌 자르기.** 최신 경기 기준 300일 창은 두 시즌을 섞는다(스위스컵 2025-26 8강·4강 + 2026-27 64강). 경계 두 개 중 늦은 쪽:
직전 결승 다음 경기 / 90일 넘는 공백(결승이 수집 안 된 시즌). 컵 겨울 휴식은 길어야 60일대(스위스 61·DFB 63).

**우승 표기.** 직전 시즌 기록만 "디펜딩 챔피언", 오래된 기록은 "기록상 최근 우승 · 2020"(코파 두 브라질 기록이 2020 에 멈춤).
끝난 시즌은 단판 결승 결과로 우승팀(DB 판정). 동점인데 승부차기 미수집이면 판정하지 않고 "결승 · 톨루카 1-1 티그레스 ·
승부차기 결과는 수집되지 않았습니다"를 그대로(CONCACAF 2026).

**라벨 함정.** ts 는 16강을 "1/8 finals"라 부른다 — 기존 roundKo 는 "final" 이 들어가면 "결승"으로 옮겨 코파 두 브라질
16강이 "결승 16경기"로 나왔다. 여정 쪽 stageKo 에서 1/N finals → 2N강, "Group Stage" → 조별리그. (대진표 쪽 roundKo 는 그대로 —
roundRank 가 이 라벨을 null 로 빼서 대진표엔 안 나온다.)

**로컬 빌드.** 오늘 이 맥 네트워크가 불안정해 사전 렌더 중 Neon P1001·구글 폰트 실패로 4회 중단. 컴파일·타입 검사는 통과.

## 코파 델 레이 대회 여정 (2026-09-24)
- 카드가 없던 이유: DB 에 2025-26 137경기뿐(8/21 일괄 적재, raw 없음)이고 새 시즌을 모으는 경로가 없었다. af collect 목록(ALL_LEAGUES)에 없고, ts 워커는 하부리그 팀이 매핑 밖이라 전량 skippedNoTeam.
- 해법: FA컵과 같은 구조라 collect-fa-cup 잡에 `--league` 를 붙여 재사용, 맥미니 daily-fa-cup.sh 에 한 줄 추가. 첫 실행으로 2026-27 예선 20경기(af 집계와 동일) 적재.
- 지난 시즌 raw 백필은 하지 않았다: ts diary·season/recent 가 지난 시즌엔 405. 대회 여정은 이번 시즌만 쓰고 디펜딩 챔피언은 기록 파일이라 불필요.
