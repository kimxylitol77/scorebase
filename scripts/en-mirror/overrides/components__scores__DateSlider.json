{
  "header": "scores__DateSlider (영어판). scripts/en-mirror 로 자동 생성 — 직접 수정하지 말 것.",
  "preReplace": [
    [
      "        const weekday = d.toLocaleDateString(\"ko-KR\", {",
      "        const weekday = d.toLocaleDateString(\"en-GB\", {"
    ],
    [
      "router.replace(`/scores?sport=${sport}${extraQuery}`);",
      "router.replace(`/en/scores?sport=${sport}${extraQuery}`);"
    ],
    [
      "href={isToday ? `/scores?sport=${sport}${extraQuery}` : `/scores?sport=${sport}&date=${ds}${extraQuery}`}",
      "href={isToday ? `/en/scores?sport=${sport}${extraQuery}` : `/en/scores?sport=${sport}&date=${ds}${extraQuery}`}"
    ]
  ],
  "dict": {
    "일자 슬라이더": "Date selector",
    "오늘": "Today"
  }
}
