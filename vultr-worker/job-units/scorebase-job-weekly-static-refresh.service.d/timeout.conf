# 2026-09-08 — 맥미니 launchd 에는 타임아웃이 없어 몇 시간짜리 주간 잡이 그대로 끝났는데,
# 9/03 systemd 이전 뒤 기본 타임아웃에 걸려 SIGKILL 로 매주 실패했다(heartbeat 도 끊겨 무감지).
# 주 1회 잡이라 다음 실행까지 7일 여유가 있어 넉넉히 잡는다.
[Service]
TimeoutStartSec=14400
